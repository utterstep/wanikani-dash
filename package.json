{
  "name": "wkdash",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd crates/wkdash-worker && worker-build --release",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:browser": "bash tests/e2e.sh"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.128.0"
  },
  "optionalDependencies": {
    "@rolldown/binding-darwin-arm64": "1.2.7"
  }
}
